'use strict';

// Register `bodyfooter` component, along with its associated controller and template
angular.
  module('bodyfooter').
  component('bodyfooter', {
    templateUrl: '../../views/bodyFooter.html',
    controller: []
  });